export const requesting = state => state.requesting;
export const branchConfigList = state => state.branchConfig.items || [];
export const totalCount = state => state.branchConfig.totalCount || 0;
export const ruleValidate = state => state.ruleValidate;
